/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./index.html", "./src/**/*.jsx"],
  theme: {
    extend: {},
  },
  plugins: [
    // Aquí puedes agregar plugins de Tailwind CSS si los necesitas
    // Por ejemplo, '@tailwindcss/forms', '@tailwindcss/typography', etc.
  ],
  variants: {
    extend: {},
  },
}
